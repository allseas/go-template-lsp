{{- /*gotype: demo/models.Order*/ -}}
{{- /*
  COMPLETIONS DEMO

  - difference between fields and methods
  - return types visible
*/ -}}

# Completion trigger A - top-level fields of Order
id:           {{ .ID }}
customer:     {{ .CustomerName }}
email:        {{ .Email }}
total:        {{ .TotalAmount }}
paid:         {{ .Paid }}
item_count:   {{ .ItemCount }}

# Completion trigger B - variables in scope


{{ $var := 1 }}

{{ range $i, $item := .Items }}

{{ end }}

{{ with .Address }}

  {{ .Zip }}

{{ end }}


# Completion trigger C - nested struct fields (Address.*)
shipping:
  street:  {{ .Address.Street }}
  city:    {{ .Address.City }}
  country: {{ .Address.Country }}
  zip:     {{ .Address.Zip }}
  line:    {{ .Address.Line }}

# Completion trigger D - pipe chain; result is int
item_summary:
  count: {{ .Items | len | numberString }}
  {{ 3 | repeat "abc" }}
