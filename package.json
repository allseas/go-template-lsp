{
  "name": "go-template-extensions",
  "version": "1.0.0",
  "description": "",
  "private": "true",
  "directories": {
    "doc": "docs"
  },
  "workspaces": ["clients/*", "scripts"],
  "scripts": {
    "build:server": "ts-node --esm scripts/build-server-binaries.ts",
    "build:vscode": "npm run build:server && (cd clients/VSCode; npm run compile)",
    "package:vscode": "npm run build:server && npm run package -w clients/VSCode",
    "watch:vscode": "(ts-node --esm scripts/watch-server-binaries.ts) | (cd clients/VSCode && npm run watch)",
    "launch:vscode": "npm run package:vscode && find . | grep '.vsix' | xargs -i code --install-extension {} && code ./test"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^25.6.0",
    "eslint": "^10.2.1",
    "prettier": "^3.8.3",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=24.8.0",
    "npm": ">=11.6.0"
  }
}
