Using golangci-lint for linting. You can install this by using your repo's package manager.

To autoformat:

```bash
golangci-lint fmt
```

to check:
```bash
golangci-lint run
```

For false positives, see [here](https://golangci-lint.run/docs/linters/false-positives/)
