workflow:
  rules:
    - when : always

stages:
  - build
  - lint
  - test

default:
  interruptible: true

variables:
  GRADLE_OPTS: "-Dorg.gradle.daemon=false"

build-image:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  variables:
    IMAGE_TAG: $CI_REGISTRY_IMAGE/jetbrains-ci:latest
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build -t $IMAGE_TAG clients/JetBrains/
    - docker push $IMAGE_TAG
  rules:
    - changes:
        - clients/JetBrains/Dockerfile
    - when: manual
      allow_failure: true

linting:
  stage: lint
  image: $CI_REGISTRY_IMAGE/jetbrains-ci:latest
  script:
    - echo "running ktlint..."
    - cd clients/JetBrains/go-text-template
    - ./gradlew ktlintCheck

testing:
  stage: test
  image: $CI_REGISTRY_IMAGE/jetbrains-ci:latest
  before_script:
    - npm install
  script:
    - echo "running tests"
    - cd clients/JetBrains/go-text-template
    - ./gradlew test --no-configuration-cache
